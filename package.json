{
  "name": "leadpages",
  "version": "1.0.0",
  "private": true,
  "description": "SEO landing pages with Supabase-backed leads + analytics",
  "scripts": {
    "build:icons": "node scripts/build-apple-touch-icon.mjs",
    "test": "node --test tests/**/*.test.js",
    "test:quote": "node --test tests/quote-system/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "sharp": "^0.34.3"
  }
}
